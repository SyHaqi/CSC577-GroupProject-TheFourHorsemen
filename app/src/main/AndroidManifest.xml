<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission for Retrofit REST API calls -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Gallery access — Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Gallery access — Android 12 and below -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SocialGoodVolunteerApp"
        android:usesCleartextTraffic="true">

        <!-- Launcher activity -->
        <activity
            android:name="com.haqi.csc577groupproject.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.haqi.csc577groupproject.LoginActivity"
            android:exported="false" />

        <activity
            android:name="com.haqi.csc577groupproject.UpdateProfileActivity"
            android:exported="false" />

        <activity
            android:name="com.haqi.csc577groupproject.CreateProfileActivity"
            android:exported="false" />

        <activity
            android:name="com.haqi.csc577groupproject.RideShareActivity"
            android:exported="false" />

    </application>

</manifest>